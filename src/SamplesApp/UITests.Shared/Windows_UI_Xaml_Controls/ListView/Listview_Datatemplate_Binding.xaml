<Page
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ListView.Listview_Datatemplate_Binding"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ListView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	<Page.Resources>
		<ResourceDictionary>
			<DataTemplate x:Key="NumberItemTemplate">
				<StackPanel>
					<TextBlock Text="{Binding}" />
					<TextBlock>
						<Run Text="Parent =" />
						<Run x:Name="DCElName" Text="{Binding Path=DataContext, ElementName=NumbersListView}" />
					</TextBlock>
				</StackPanel>
			</DataTemplate>
		</ResourceDictionary>
	</Page.Resources>

	<StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<TextBlock FontSize="20"
				   Margin="20"
				   TextWrapping="Wrap">
			<Run Text="DataContext =" />
			<Run Text="{Binding Path=DataContext, ElementName=NumbersListView}" />
		</TextBlock>

		<ListView x:Name="NumbersListView" 
				  ItemsSource="{Binding Numbers}"
				  ItemTemplate="{StaticResource NumberItemTemplate}" />
	</StackPanel>
</Page>
